
export const metadata = {
    title:"Signup || Signup Page",
    description:"Signup (Register) page of ghazna.dev. Enter your valid credientials to create your account"
}

export default async function RegisterRootLayout({children}) {
    return <>{children}</>
}