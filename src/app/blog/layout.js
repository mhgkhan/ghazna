

export const metadata = {
    title : "Blog || explorer the latest blogs, articles on the modren world",
    description:"this is the description"
}

export default async function BlogRootLayout({ children }) {
    return <>{children}</>
}